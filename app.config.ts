export default defineAppConfig({
  ui: {
    colors: {
      primary: 'terracotta',
      neutral: 'stone'
    },
    button: {
      slots: {
        base: 'rounded-sm font-medium'
      },
      defaultVariants: {
        size: 'sm'
      }
    }
  }
})
